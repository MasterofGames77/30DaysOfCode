// Loops

function main() {
    const n = parseInt(2);
    for (let i = 1; i <= 10; i++) {
        // Testing
        //let result = n * i
        //console.log(result)
        console.log(n.toString() + " x " + i + " = " + result.toString())
    }
}

main()